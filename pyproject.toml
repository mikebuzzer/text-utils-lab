[project]
name = "text-utils-lab"
version = "0.1.0"
description = "Simple text utilities for Python"
authors = [{name = "Mike"}]
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
